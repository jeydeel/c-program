#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main(){
	int timeValue;
	int num;
	
	timeValue = time(NULL);
	srand (timeValue);
	
	for(int x = 0; x < 10; x++) {
		num = rand();
		cout<<"#"<<x<<": "<<num<<endl;
		
	}
}
